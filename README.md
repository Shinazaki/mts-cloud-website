# MTS Cloud Portal

Веб-панель управления облачными серверами в стиле MTS Cloud. Тёмно-красный дизайн, полная навигация, мок-авторизация и управление серверами без необходимости запуска бэкенда.

---

## 🚀 Быстрый старт

```bash
# Установите зависимости (один раз)
npm install

# Запустите сервер разработки
npm run dev
```

Откройте **http://localhost:5173/** в браузере.

> Если порт 5173 занят — Vite автоматически выберет следующий (5174, 5175...).

---

## 🔑 Авторизация

Сайт работает в **мок-режиме** — реальный бэкенд не нужен.

| Поле | Значение |
|---|---|
| Логин | любой (например `admin`) |
| Пароль | любой (например `password`) |
| Имя / Фамилия | любые (мин. 2 символа) |

После входа токен сохраняется в `localStorage`. При обновлении страницы сессия сохраняется.

---

## 📋 Страницы и функционал

### 🔐 Аутентификация
| Страница | URL | Описание |
|---|---|---|
| Вход | `/login` | Форма входа по логину и паролю |
| Регистрация | `/register` | Форма с полями Имя, Фамилия, Логин, E-mail, Пароль |

---

### 🖥 Управление серверами

#### Список серверов — `/servers`
- Таблица с серверами (название, IP, дата создания)
- 🔍 **Поиск** по названию в реальном времени
- **Расширить** → переход на страницу смены характеристик
- **Ещё ▾** → выпадающее меню: Терминал, Ресурсы, Уничтожить
- Клик по названию сервера → страница деталей

#### Детали сервера — `/servers/:id`
- IP-адрес, ОС, статус (Running), аптайм
- Конфигурация (CPU, RAM, Disk)
- История изменений сервера
- Кнопки: Перезапустить, Конфигурация

#### Изменение конфигурации — `/servers/:id/config`
- Выпадающие списки для выбора:
  - **CPU**: 1 / 2 / 4 / 8 / 16 / 32 vCPU
  - **RAM**: 1 / 2 / 4 / 8 / 16 / 32 / 64 GB
  - **Disk**: 20 / 40 / 60 / 100 / 200 / 500 / 1000 GB NVMe
- Кнопка «Сохранить» — применяет выбранную конфигурацию

#### Создание сервера — `/servers/create`
- Выбор: регион (Минск / Москва), ОС (Ubuntu / Debian / CentOS)
- Ввод названия
- Кнопка «Создать»

---

### 💳 Счета и оплата — `/billing`

Три вкладки:

| Вкладка | Содержимое |
|---|---|
| Общее | Текущий баланс, кнопка пополнения |
| История оплат | Таблица транзакций |
| Настройки оплаты | Форма (ФИО, адрес, индекс) + список карт |

В настройках оплаты:
- ➕ Добавить карту (номер, срок, CVV)
- ✕ Удалить карту
- Кнопка «Сохранить» с визуальным индикатором

---

### 🎧 Поддержка — `/support`
- Форма создания тикета (тема, категория, описание)
- Список ранее созданных обращений

---

### ⚙️ Настройки — `/settings`
- **Язык**: Русский / Английский (применяется мгновенно ко всему сайту)
- **Тема**: Светлая / Тёмная / Системная (сохраняется в `localStorage`)
- **Безопасность**: заглушка 2FA

---

### Остальные страницы
| Страница | URL | Описание |
|---|---|---|
| Бэкапы | `/backups` | Управление резервными копиями |
| Мониторинг | `/monitoring` | Метрики CPU / RAM / Disk |
| Трафик | `/traffic` | Статистика сети |
| API | `/api` | Управление токенами API |
| Q&A | `/qa` | Часто задаваемые вопросы |
| Что нового? | `/whats-new` | Список обновлений |

---

## 🏗 Структура проекта

```
src/
├── Pages/
│   ├── Auth/           # Login, Register
│   ├── Servers/        # Servers, ServerDetails, ServerConfig, CreateServer
│   ├── Billing/        # Billing (3 вкладки)
│   ├── Settings/       # Settings (язык, тема, 2FA)
│   ├── Support/        # Support (тикеты)
│   └── SecondaryPages/ # Backups, Monitoring, Traffic, API, QA, WhatsNew
├── components/
│   ├── Header/         # Фиксированная шапка (Конфигурация, иконки, аккаунт)
│   └── Sidebar/        # Фиксированная боковая панель
├── layouts/
│   ├── DashboardLayout # Обёртка для авторизованных страниц
│   └── AuthLayout      # Обёртка для страниц входа/регистрации
├── contexts/
│   ├── AuthProvider    # Управление JWT-токеном
│   └── SettingsContext # Язык и тема (localStorage)
├── hooks/
│   ├── useAuth         # Хук авторизации
│   └── useSettings     # Хук настроек + функция t(key)
├── i18n/
│   ├── ru.ts           # Русский словарь
│   └── en.ts           # Английский словарь
├── api/
│   └── client.ts       # Axios + мок-заглушки для офлайн-работы
└── types/
    └── auth.ts         # Интерфейсы User, LoginDto, AuthResponse
```

---

## 🛠 Стек технологий

| Технология | Версия | Назначение |
|---|---|---|
| React | 19 | UI-фреймворк |
| TypeScript | 5.9 | Статическая типизация |
| Vite | 7 | Сборщик и dev-сервер |
| React Router | 7 | Клиентский роутинг |
| Axios | 1.x | HTTP-запросы (+ мок-режим) |
| Material Symbols | (CDN) | Иконки |
| Google Fonts (Nunito) | (CDN) | Типографика |

---

## 🔧 Бэкенд (опционально)

Если хотите запустить реальный бэкенд:

```bash
# В отдельном терминале
cd spacehack-mts-cloud-backend
npm install
npm run start:dev   # http://localhost:8080/api
```

> Для бэкенда нужна база данных PostgreSQL. Без неё сайт работает через мок-заглушки.

---

## 🌍 Локализация

Смена языка на странице **Настройки → Язык интерфейса** мгновенно переключает весь интерфейс.  
Настройка сохраняется при перезагрузке страницы.

---

## 🎨 Дизайн

- **Цветовая схема**: Тёмно-красный (#8B0000), красный (#C1121F), белый, серый
- **Шрифт**: Nunito (Google Fonts)
- **Иконки**: Material Symbols Outlined
- **Боковая панель**: `position: fixed`, ширина 250px
- **Шапка**: `position: fixed`, высота 64px, z-index 999
